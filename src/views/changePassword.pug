extends base

block main
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(name="oldPassword", type="password", placeholder="Old Password", required)
        input(name="newPassword", type="password", placeholder="New Password", required)
        input(name="newPasswordConfirm", type="password", placeholder="New Password Confirmation", required)
        button Submit